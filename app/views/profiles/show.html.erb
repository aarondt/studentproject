<h1>Dein Profil</h1>

    <ul class="listing">
        <div class="row">
            <div class="well col-md-4 col-md-offset-4">
                <li><%= "#{@profile.name}" %>
                </li>
                <li><%= "#{@profile.bio}" %>
                </li>
                <li><%= "#{@profile.specialization}" %>
                </li>
            </div>
        </div>
    </ul>
</br>
<%= link_to 'Profil bearbeiten', edit_profile_path %>